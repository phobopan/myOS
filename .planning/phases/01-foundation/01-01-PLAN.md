---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - phoebeOS/phoebeOSApp.swift
  - phoebeOS/ContentView.swift
  - phoebeOS/Components/VisualEffectView.swift
  - phoebeOS/Views/GlassCard.swift
autonomous: true

must_haves:
  truths:
    - "App launches showing a transparent window with frosted glass effect"
    - "Desktop is visible through the window but heavily blurred"
    - "Window can be dragged by clicking anywhere on the background"
  artifacts:
    - path: "phoebeOS/phoebeOSApp.swift"
      provides: "App entry point with window configuration"
      contains: ".windowStyle(.hiddenTitleBar)"
    - path: "phoebeOS/Components/VisualEffectView.swift"
      provides: "NSVisualEffectView wrapper for window transparency"
      exports: ["VisualEffectView"]
    - path: "phoebeOS/Views/GlassCard.swift"
      provides: "Reusable glass container component"
      exports: ["GlassCard"]
    - path: "phoebeOS/ContentView.swift"
      provides: "Main content view with background effect"
      contains: "VisualEffectView"
  key_links:
    - from: "phoebeOS/ContentView.swift"
      to: "phoebeOS/Components/VisualEffectView.swift"
      via: "import and usage as background"
      pattern: "VisualEffectView.*ignoresSafeArea"
---

<objective>
Create the Xcode project foundation with glassmorphism core components.

Purpose: Establish the transparent window with frosted glass effect that is the visual signature of phoebeOS.
Output: Working Xcode project with VisualEffectView wrapper, GlassCard component, and proper window configuration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with window configuration</name>
  <files>
    phoebeOS/phoebeOSApp.swift
    phoebeOS/ContentView.swift
    phoebeOS.xcodeproj/
  </files>
  <action>
Create a new macOS SwiftUI app using the swift package manager or Xcode project structure:

1. Create the Xcode project structure manually or use `xcodegen` if available. The project should be named "phoebeOS" with:
   - macOS deployment target: 14.0
   - Swift language version: 5
   - SwiftUI lifecycle

2. Configure phoebeOSApp.swift:
```swift
import SwiftUI

@main
struct phoebeOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.hiddenTitleBar)
        .windowBackgroundDragBehavior(.enabled)
        .defaultSize(width: 1200, height: 800)
        .windowResizability(.contentMinSize)
    }
}
```

3. Create initial ContentView.swift as a placeholder:
```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("phoebeOS")
            .font(.largeTitle)
            .foregroundStyle(.white)
            .frame(minWidth: 900, minHeight: 600)
    }
}

#Preview {
    ContentView()
}
```

Use `swift package init --type executable` and then convert to Xcode project, OR create the xcodeproj directly using a manual approach that works for macOS SwiftUI apps.
  </action>
  <verify>
Run `xcodebuild -list -project phoebeOS.xcodeproj` to verify project structure exists.
Run `xcodebuild -scheme phoebeOS -destination 'platform=macOS' build` to verify it compiles.
  </verify>
  <done>Xcode project exists at /Users/Phoebe/Desktop/phoebeOS/phoebeOS.xcodeproj with hidden titlebar, background drag enabled, default size 1200x800, minimum size 900x600.</done>
</task>

<task type="auto">
  <name>Task 2: Create VisualEffectView wrapper and GlassCard component</name>
  <files>
    phoebeOS/Components/VisualEffectView.swift
    phoebeOS/Views/GlassCard.swift
    phoebeOS/ContentView.swift
  </files>
  <action>
1. Create the Components directory and VisualEffectView.swift:

```swift
import SwiftUI
import AppKit

/// NSViewRepresentable wrapper for NSVisualEffectView to achieve window-level blur
struct VisualEffectView: NSViewRepresentable {
    var material: NSVisualEffectView.Material = .hudWindow
    var blendingMode: NSVisualEffectView.BlendingMode = .behindWindow

    func makeNSView(context: Context) -> NSVisualEffectView {
        let effectView = NSVisualEffectView()
        effectView.material = material
        effectView.blendingMode = blendingMode
        effectView.state = .active  // Always active, don't follow window state
        return effectView
    }

    func updateNSView(_ nsView: NSVisualEffectView, context: Context) {
        nsView.material = material
        nsView.blendingMode = blendingMode
    }
}
```

2. Create the Views directory and GlassCard.swift:

```swift
import SwiftUI

/// Reusable glass container with layered transparency effect
struct GlassCard<Content: View>: View {
    let content: Content
    var cornerRadius: CGFloat = 16

    init(cornerRadius: CGFloat = 16, @ViewBuilder content: () -> Content) {
        self.cornerRadius = cornerRadius
        self.content = content()
    }

    var body: some View {
        content
            .background(.thinMaterial)
            .clipShape(RoundedRectangle(cornerRadius: cornerRadius, style: .continuous))
            .overlay(
                RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                    .stroke(Color.white.opacity(0.2), lineWidth: 1)
            )
            .shadow(color: .black.opacity(0.1), radius: 10, x: 0, y: 4)
    }
}

#Preview {
    ZStack {
        Color.blue.opacity(0.5)
        GlassCard {
            Text("Sample Content")
                .foregroundStyle(.white)
                .padding()
        }
        .padding()
    }
    .frame(width: 300, height: 200)
}
```

3. Update ContentView.swift to use the VisualEffectView as background:

```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            // Main content area - placeholder for now
            VStack {
                Text("phoebeOS")
                    .font(.largeTitle)
                    .fontWeight(.medium)
                    .foregroundStyle(.white)

                Text("Foundation Ready")
                    .font(.subheadline)
                    .foregroundStyle(.white.opacity(0.7))
            }
        }
        .frame(minWidth: 900, minHeight: 600)
        .background(VisualEffectView().ignoresSafeArea())
    }
}

#Preview {
    ContentView()
}
```

Ensure the Components/ and Views/ directories are added to the Xcode project target.
  </action>
  <verify>
Run `xcodebuild -scheme phoebeOS -destination 'platform=macOS' build` to verify compilation.
Check that VisualEffectView.swift and GlassCard.swift exist in correct locations.
  </verify>
  <done>VisualEffectView wrapper provides window-level blur with .hudWindow material and .behindWindow blending. GlassCard component provides .thinMaterial background with rounded corners and subtle border. ContentView uses VisualEffectView as background.</done>
</task>

</tasks>

<verification>
1. Project compiles without errors: `xcodebuild -scheme phoebeOS build`
2. Run the app - window should show transparent background with heavy blur
3. Desktop is visible but heavily frosted (shapes unrecognizable)
4. Window can be dragged by clicking anywhere
5. Window has no visible titlebar
</verification>

<success_criteria>
- Xcode project created with proper SwiftUI macOS configuration
- Window launches with transparent glassmorphism background
- VisualEffectView wrapper exists and functions correctly
- GlassCard component exists and provides layered glass effect
- Window supports resize, minimize, close (standard macOS behaviors)
- Window drag works from background area
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
