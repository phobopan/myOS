---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/renderer/App.tsx
  - src/renderer/components/Sidebar.tsx
  - src/renderer/components/ConversationList.tsx
  - src/renderer/components/ThreadView.tsx
  - src/renderer/components/Titlebar.tsx
  - src/renderer/components/Settings.tsx
  - src/renderer/index.css
autonomous: true

must_haves:
  truths:
    - "User sees two-pane layout with message list on left and thread view on right"
    - "User sees a draggable titlebar area that does not interfere with traffic lights"
    - "User can open settings panel from the UI"
    - "Settings panel shows placeholders for notifications and accounts"
    - "Empty states display helpful text (no conversations, select a conversation)"
  artifacts:
    - path: "src/renderer/components/Sidebar.tsx"
      provides: "Left pane container with conversation list"
      exports: ["Sidebar"]
    - path: "src/renderer/components/ConversationList.tsx"
      provides: "Scrollable list of conversations"
      exports: ["ConversationList"]
    - path: "src/renderer/components/ThreadView.tsx"
      provides: "Right pane showing selected conversation thread"
      exports: ["ThreadView"]
    - path: "src/renderer/components/Titlebar.tsx"
      provides: "Custom titlebar with drag region"
      exports: ["Titlebar"]
    - path: "src/renderer/components/Settings.tsx"
      provides: "Settings modal/panel"
      exports: ["Settings"]
  key_links:
    - from: "src/renderer/App.tsx"
      to: "src/renderer/components/Sidebar.tsx"
      via: "import and render"
      pattern: "import.*Sidebar"
    - from: "src/renderer/App.tsx"
      to: "src/renderer/components/ThreadView.tsx"
      via: "import and render"
      pattern: "import.*ThreadView"
    - from: "src/renderer/components/Titlebar.tsx"
      to: "window.electron.titlebarDoubleClick"
      via: "onDoubleClick handler"
      pattern: "titlebarDoubleClick"
---

<objective>
Build the two-pane layout with glassmorphism styling and settings panel

Purpose: Create the visual shell that will house all messaging features. This establishes the UI patterns (glass panels, white text, sidebar + detail split) that all future components follow.

Output: A two-pane interface with empty message list on left, empty thread view on right, custom titlebar, and accessible settings panel with placeholder content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ELECTRON-GLASSMORPHISM.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create two-pane layout with glass styling</name>
  <files>
    src/renderer/App.tsx
    src/renderer/components/Titlebar.tsx
    src/renderer/components/Sidebar.tsx
    src/renderer/components/ConversationList.tsx
    src/renderer/components/ThreadView.tsx
    src/renderer/index.css
  </files>
  <action>
**Component Structure:**
```
App.tsx
  |-- Titlebar
  |-- main container (flex row)
        |-- Sidebar (w-80, fixed width)
        |     |-- ConversationList
        |
        |-- ThreadView (flex-1, fills remaining space)
```

**src/renderer/components/Titlebar.tsx:**
```tsx
export function Titlebar() {
  const handleDoubleClick = (e: React.MouseEvent) => {
    // Only trigger if clicking the titlebar itself, not buttons
    if (e.target === e.currentTarget) {
      window.electron.titlebarDoubleClick();
    }
  };

  return (
    <div
      className="titlebar h-[52px] flex items-center px-4"
      onDoubleClick={handleDoubleClick}
    >
      {/* Left padding for traffic lights */}
      <div className="w-[70px]" />

      <span className="text-sm font-semibold text-white/80">
        phoebeOS
      </span>

      {/* Settings button on right */}
      <div className="ml-auto">
        {/* Settings button will be added in Task 2 */}
      </div>
    </div>
  );
}
```

CSS for titlebar (in index.css):
```css
.titlebar {
  -webkit-app-region: drag;
  -webkit-user-select: none;
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.titlebar button {
  -webkit-app-region: no-drag;
}
```

**src/renderer/components/Sidebar.tsx:**
```tsx
export function Sidebar() {
  return (
    <aside className="w-80 h-full flex flex-col glass-panel border-r border-white/10">
      {/* Filter tabs will go here in Phase 5 */}
      <div className="p-3 border-b border-white/10">
        <span className="text-xs text-white/50 uppercase tracking-wider">
          Messages
        </span>
      </div>

      <ConversationList />
    </aside>
  );
}
```

**src/renderer/components/ConversationList.tsx:**
```tsx
export function ConversationList() {
  // Empty state for now - Phase 2 will populate with real data
  return (
    <div className="flex-1 overflow-y-auto">
      <div className="flex flex-col items-center justify-center h-full text-white/40 p-8">
        <div className="text-4xl mb-4">inbox zero</div>
        <p className="text-sm text-center">
          No messages awaiting reply
        </p>
      </div>
    </div>
  );
}
```

**src/renderer/components/ThreadView.tsx:**
```tsx
export function ThreadView() {
  // Empty state - no conversation selected
  return (
    <main className="flex-1 h-full flex flex-col glass-panel">
      <div className="flex-1 flex items-center justify-center text-white/30">
        <div className="text-center">
          <div className="text-6xl mb-4">Select a conversation</div>
          <p className="text-sm">
            Choose a message from the sidebar to view the thread
          </p>
        </div>
      </div>
    </main>
  );
}
```

**src/renderer/App.tsx:**
```tsx
import { Titlebar } from './components/Titlebar';
import { Sidebar } from './components/Sidebar';
import { ThreadView } from './components/ThreadView';

export function App() {
  return (
    <div className="h-screen flex flex-col bg-transparent text-white">
      <Titlebar />

      <div className="flex-1 flex overflow-hidden">
        <Sidebar />
        <ThreadView />
      </div>
    </div>
  );
}
```

**src/renderer/index.css additions:**
```css
/* Glass panel base style */
.glass-panel {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

/* Scrollbar styling for dark theme */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.2);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.3);
}
```
  </action>
  <verify>
```bash
npm run dev
```
Visual verification:
1. Two-pane layout visible (sidebar on left, main content on right)
2. Titlebar is draggable (can move window by dragging titlebar)
3. Traffic lights still visible and not obscured
4. Glass effect on panels (slight blur, semi-transparent)
5. White text readable on glass background
6. Empty states display correctly
  </verify>
  <done>
Two-pane layout renders with sidebar (w-80) and thread view (flex-1). Titlebar is draggable. Glass styling applied with backdrop-filter. Empty states show placeholder text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Settings panel with placeholder content</name>
  <files>
    src/renderer/components/Settings.tsx
    src/renderer/components/Titlebar.tsx
    src/renderer/App.tsx
  </files>
  <action>
**src/renderer/components/Settings.tsx:**
```tsx
import { useState } from 'react';

interface SettingsProps {
  isOpen: boolean;
  onClose: () => void;
}

export function Settings({ isOpen, onClose }: SettingsProps) {
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center">
      {/* Backdrop */}
      <div
        className="absolute inset-0 bg-black/50 backdrop-blur-sm"
        onClick={onClose}
      />

      {/* Modal */}
      <div className="relative glass-modal w-[500px] max-h-[600px] rounded-2xl overflow-hidden">
        {/* Header */}
        <div className="flex items-center justify-between p-4 border-b border-white/10">
          <h2 className="text-lg font-semibold text-white">Settings</h2>
          <button
            onClick={onClose}
            className="text-white/60 hover:text-white transition-colors"
          >
            <span className="text-2xl">&times;</span>
          </button>
        </div>

        {/* Content */}
        <div className="p-4 space-y-6 overflow-y-auto max-h-[500px]">
          {/* Accounts Section */}
          <section>
            <h3 className="text-sm font-medium text-white/70 uppercase tracking-wider mb-3">
              Connected Accounts
            </h3>
            <div className="space-y-2">
              <AccountRow
                name="iMessage"
                status="pending"
                description="Requires Full Disk Access"
              />
              <AccountRow
                name="Gmail"
                status="pending"
                description="Not connected"
              />
              <AccountRow
                name="Instagram"
                status="pending"
                description="Not connected"
              />
            </div>
          </section>

          {/* Notifications Section */}
          <section>
            <h3 className="text-sm font-medium text-white/70 uppercase tracking-wider mb-3">
              Notifications
            </h3>
            <div className="space-y-2">
              <ToggleRow
                label="Desktop notifications"
                description="Show system notifications for new messages"
                disabled
              />
              <ToggleRow
                label="Badge count"
                description="Show unread count on dock icon"
                disabled
              />
            </div>
          </section>

          {/* About Section */}
          <section>
            <h3 className="text-sm font-medium text-white/70 uppercase tracking-wider mb-3">
              About
            </h3>
            <p className="text-sm text-white/50">
              phoebeOS v0.1.0 (Foundation)
            </p>
          </section>
        </div>
      </div>
    </div>
  );
}

function AccountRow({
  name,
  status,
  description
}: {
  name: string;
  status: 'connected' | 'pending' | 'error';
  description: string;
}) {
  const statusColors = {
    connected: 'bg-green-500',
    pending: 'bg-yellow-500',
    error: 'bg-red-500',
  };

  return (
    <div className="flex items-center justify-between p-3 rounded-lg bg-white/5">
      <div className="flex items-center gap-3">
        <div className={`w-2 h-2 rounded-full ${statusColors[status]}`} />
        <div>
          <div className="text-sm font-medium text-white">{name}</div>
          <div className="text-xs text-white/50">{description}</div>
        </div>
      </div>
      <button
        className="text-xs text-white/60 hover:text-white transition-colors px-3 py-1 rounded bg-white/10"
        disabled
      >
        Configure
      </button>
    </div>
  );
}

function ToggleRow({
  label,
  description,
  disabled = false
}: {
  label: string;
  description: string;
  disabled?: boolean;
}) {
  return (
    <div className={`flex items-center justify-between p-3 rounded-lg bg-white/5 ${disabled ? 'opacity-50' : ''}`}>
      <div>
        <div className="text-sm font-medium text-white">{label}</div>
        <div className="text-xs text-white/50">{description}</div>
      </div>
      <div className="w-10 h-6 rounded-full bg-white/20 relative cursor-not-allowed">
        <div className="absolute left-1 top-1 w-4 h-4 rounded-full bg-white/60 transition-transform" />
      </div>
    </div>
  );
}
```

**Update src/renderer/components/Titlebar.tsx:**
Add settings button that triggers modal:
```tsx
interface TitlebarProps {
  onSettingsClick: () => void;
}

export function Titlebar({ onSettingsClick }: TitlebarProps) {
  // ... existing code ...

  {/* Settings button on right */}
  <button
    onClick={onSettingsClick}
    className="p-2 rounded-lg hover:bg-white/10 transition-colors text-white/60 hover:text-white"
    title="Settings"
  >
    {/* Gear icon - use simple SVG or unicode */}
    <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
    </svg>
  </button>
}
```

**Update src/renderer/App.tsx:**
```tsx
import { useState } from 'react';
import { Titlebar } from './components/Titlebar';
import { Sidebar } from './components/Sidebar';
import { ThreadView } from './components/ThreadView';
import { Settings } from './components/Settings';

export function App() {
  const [settingsOpen, setSettingsOpen] = useState(false);

  return (
    <div className="h-screen flex flex-col bg-transparent text-white">
      <Titlebar onSettingsClick={() => setSettingsOpen(true)} />

      <div className="flex-1 flex overflow-hidden">
        <Sidebar />
        <ThreadView />
      </div>

      <Settings
        isOpen={settingsOpen}
        onClose={() => setSettingsOpen(false)}
      />
    </div>
  );
}
```

**Add modal glass styling to index.css:**
```css
.glass-modal {
  background: rgba(30, 30, 30, 0.85);
  backdrop-filter: blur(40px) saturate(180%);
  -webkit-backdrop-filter: blur(40px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
}
```
  </action>
  <verify>
```bash
npm run dev
```
Visual verification:
1. Settings gear icon visible in titlebar (right side)
2. Clicking gear opens settings modal
3. Modal shows three sections: Accounts, Notifications, About
4. All three accounts show "pending" status
5. Toggle switches are disabled (placeholder state)
6. Clicking backdrop or X closes modal
7. Modal has glass effect (darker, more blur than panels)
  </verify>
  <done>
Settings panel opens from titlebar. Shows three sections with placeholder content. Accounts section lists iMessage, Gmail, Instagram with pending status. Notifications section shows disabled toggles. Modal closes via backdrop click or X button.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - app launches with two-pane layout
2. Sidebar takes left 320px, thread view fills remaining space
3. Titlebar is draggable, traffic lights not obscured
4. Empty states show appropriate placeholder messages
5. Settings gear visible and clickable
6. Settings modal opens with placeholder content for all three accounts
7. Settings modal closes via backdrop or X
8. All elements have white text on glass backgrounds
9. Window still shows glassmorphism effect
</verification>

<success_criteria>
- Two-pane layout: sidebar (320px) + thread view (flexible)
- Custom titlebar with drag region and settings button
- Settings modal with Connected Accounts (iMessage/Gmail/Instagram) and Notifications sections
- Glass styling applied to all panels (backdrop-filter blur)
- Empty states with helpful placeholder text
- All text is white/white-alpha for glassmorphism legibility
- Meets SHELL-02 (two-pane), SHELL-03 (resize/minimize via traffic lights), SHELL-04 (settings panel)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
